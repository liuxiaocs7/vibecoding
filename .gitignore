# Go binary / build artifacts
/vibecoding
/vibecoding.exe
/bin/
*.exe
*.test
*.out
*.prof
*.coverprofile
coverage.*
cover.out
cover.html
__debug_bin*

# Go module vendor tree (regenerate locally with: go mod vendor)
/vendor/

# Release / packaging (make release → dist/release/)
/dist/

# Frontend build + deps (Vite)
/web/node_modules/
/web/dist/
/web/.vite/
/web/*.local
/web/.eslintcache
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Embedded copy is regenerated by `make sync-web` / `make build`
/cmd/vibecoding/dist/

# Env / secrets
.env
.env.*
!.env.example
!**/.env.example
*.pem
*.key

# Local data (SQLite + worktrees) if someone points -data here
/vibecoding-data/
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# Logs
*.log

# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/
*.sublime-project
*.sublime-workspace
